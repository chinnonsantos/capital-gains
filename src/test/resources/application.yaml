env: ${ENV:test}
debug: ${DEBUG:false}
trace: ${TRACE:false}

app:
  rounding-mode: HALF_UP
  scale: 2
  operation-ceiling: 20000.00
  tax-percentage: 0.20

spring:
  application:
    name: ${APP_NAME:capital-gains}
  main:
    banner-mode: off
    web-application-type: NONE
